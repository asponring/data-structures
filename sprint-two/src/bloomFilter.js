var BloomFilter = function(size) {

};

BloomFilter.prototype.hashFunctions = [];

BloomFilter.prototype.add = function() {

};

BloomFilter.prototype.contains = function(val)

var bloomFilter = new BloomFilter(18);
